# ==============================================================================
# DEPRECATED - kept only so an old command does not silently install nothing.
#
# Requirements are split by role, and each file is pinned to the transitive
# closure actually verified to work:
#
#   requirements-prod.txt    serving (FastAPI, LangGraph, Qdrant, guardrails)
#   requirements-ingest.txt  ingestion extras (Office parsers)
#   requirements-eval.txt    evaluation (ragas and its shim dependency)
#   requirements-dev.txt     tests and linting
#
# This file previously listed google-generativeai and langchain-google-genai for
# "Gemini embeddings" that the code stopped using, and described
# sentence-transformers as something FlashRank uses internally (it does not -
# FlashRank runs ONNX; sentence-transformers is the embedding model). Drift like
# that is how a dependency prune removes the wrong thing.
# ==============================================================================

-r requirements-prod.txt
-r requirements-dev.txt
