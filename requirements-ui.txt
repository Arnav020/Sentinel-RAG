# ==============================================================================
# STREAMLIT UI REQUIREMENTS
#
# The UI is a thin HTTP client: it POSTs to the backend's /query and renders the
# response. It performs no retrieval, no embedding and no LLM calls, so it needs
# none of the ML stack. Keeping it in its own image avoids shipping ~1 GB of
# torch to a container that only draws a chat window.
# ==============================================================================

streamlit==1.61.1
requests==2.34.2
python-dotenv==1.2.2
logfire==4.40.0
