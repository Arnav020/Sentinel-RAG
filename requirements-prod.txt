# ==============================================================================
# BACKEND SERVING REQUIREMENTS  (FastAPI + LangGraph query path)
#
# Scope: exactly what `app/` imports on the request path — verified by AST scan
# of every module under app/, not by hand.
#
# Deliberately EXCLUDED:
#   * unstructured / python-docx / python-pptx  → only reached by the lazy
#     import in ingestion/processor.py for .docx/.pptx. Ingestion is a batch
#     job, so those live in requirements-ingest.txt and stay out of the
#     serving image (they are heavy).
#   * ragas / deepeval / streamlit → eval and UI only.
#
# torch is installed separately from the CPU wheel index in the Dockerfile.
# Installing it from PyPI pulls the CUDA build (~2.5 GB of GPU libraries this
# workload never uses).
# ==============================================================================

# --- CORE API ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-dotenv==1.2.2
requests==2.34.2
numpy==2.5.2
pydantic==2.13.4

# --- VECTOR DB & RETRIEVAL ---
qdrant-client==1.19.0
FlashRank==0.2.10
sentence-transformers==5.7.0      # local embeddings — the app cannot answer without this

# --- LANGCHAIN & AGENTIC ORCHESTRATION ---
langchain==1.3.15
langgraph==1.2.11
langchain-groq==1.1.3
langchain-openai==1.4.3           # OpenAI-compatible client used to reach Portkey
langchain-community==0.4.2

# --- GUARDRAILS ---
nemoguardrails==0.23.0            # 0.23+ required: structured activated_rails + rails=["input"]
groq==0.37.1                      # direct client for Prompt Guard + topic filter

# --- LLM GATEWAY ---
portkey-ai==2.3.4

# --- OBSERVABILITY ---
logfire[fastapi,requests]==4.40.0
langsmith==0.8.3

# --- INGESTION PARSERS (query path only: PDF/HTML/TXT) ---
pypdf==6.15.0
pdfplumber==0.11.10
beautifulsoup4==4.15.0
